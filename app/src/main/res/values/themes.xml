<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.ParamedQuiz" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Night-aware, żeby okno po splashu nie było białe (patrz values-night/colors.xml). -->
        <item name="android:windowBackground">@color/window_background</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/mediquiz_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.ParamedQuiz</item>
    </style>
</resources>